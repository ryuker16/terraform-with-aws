{
  "logging": {
    "connection": {
      "region": "AWS_REGION",
      "credentials": {
        "accessKeyId": "AWS_ACCESS_KEY",
        "secretAccessKey": "AWS_ACCESS_SECRET"
      }
    },
    "stream": {
      "name": "KINESIS_STREAM_NAME"
    }
  }
}
