"use strict";
const logentries = require('le_node');
function getLogEntriesBunyanStream(options) {
    if (!options) {
        throw new TypeError('options');
    }
    return logentries.bunyanStream(options);
}
exports.getLogEntriesBunyanStream = getLogEntriesBunyanStream;
