/// <reference types="mocha" />

import { expect } from 'chai'
import * as subject from '../src'
import * as Queues from '../src/queues'
import * as Aws from '../src/aws'

describe('require(infrastructure-node-cloudservices-lib)', function () {
  it('should export the Queues module', function () {
    expect(subject.Queues).to.be.ok
    expect(Queues).to.be.ok
  })
  it('should export the Aws module', function () {
    expect(subject.Aws).to.be.ok
    expect(Aws).to.be.ok
  })
})
