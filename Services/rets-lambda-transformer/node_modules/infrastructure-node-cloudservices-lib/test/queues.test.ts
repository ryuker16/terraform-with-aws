/// <reference types="mocha" />

import { expect } from 'chai'
import * as subject from '../src/queues'

describe('require(infrastructure-node-cloudservices-lib/queues)', function () {
  it('should export the QueueService class', function () {
    expect(subject.QueueService).to.be.a('function')
    expect(subject.QueueService.prototype).to.be.ok
  })
  it('should export the QueueConsumer class', function () {
    expect(subject.QueueConsumer).to.be.a('function')
    expect(subject.QueueConsumer.prototype).to.be.ok
  })
})
